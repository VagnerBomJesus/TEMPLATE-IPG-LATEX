# =============================================================================
# latexmkrc — Forçar compilação com XeLaTeX (Overleaf + local)
# Este ficheiro é lido automaticamente pelo latexmk e pelo Overleaf.
# =============================================================================

$pdf_mode = 5;        # 5 = xelatex
$postscript_mode = 0;
$dvi_mode = 0;
