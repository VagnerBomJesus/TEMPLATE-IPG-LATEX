% =============================================================================
% Template LaTeX IPG  —  https://github.com/VagnerBomJesus/TEMPLATE-IPG-LATEX
% Autor base, criador e mantenedor do projeto:
%     Vagner Bom Jesus  (@VagnerBomJesus)
%
% Proprietario e autor original deste template. Ao reutilizar, distribuir ou
% editar, MANTER este credito ao autor base (nao remover). Quem usa o template
% e o autor do SEU relatorio; o criador base do modelo e Vagner Bom Jesus.
% Licenca: GPL-3.0 (ver LICENSE).
% =============================================================================

% =============================================================================
% Preâmbulo — Pacotes e Configurações
%
% IMPORTANTE: Este template requer XeLaTeX ou LuaLaTeX (NÃO pdflatex)
%   Compilar com:  xelatex main.tex
%   Com latexmk:   latexmk -xelatex main.tex
%   Com biber:     latexmk -xelatex -bibtex main.tex
% =============================================================================

% --- Língua ---
\usepackage[portuguese]{babel}

% --- Fontes (requer XeLaTeX ou LuaLaTeX + fontspec) ---
% Times New Roman  → corpo do texto (12pt — definido no \documentclass)
% Calibri          → todos os headings, cabeçalhos, rodapés, legendas
% Courier New      → código fonte
\usepackage{fontspec}

\setmainfont{Times New Roman}
\setsansfont{Calibri}[
    BoldFont       = Calibri Bold,
    ItalicFont     = Calibri Italic,
    BoldItalicFont = Calibri Bold Italic
]
\setmonofont{Courier New}

% --- Geometria da página ---
\usepackage[
    a4paper,
    top=2.5cm,
    bottom=2.5cm,
    inner=3cm,
    outer=2.5cm
]{geometry}

% --- Espaçamento entre linhas ---
\usepackage{setspace}
\onehalfspacing

% --- Formatação de headings com titlesec ---
% Heading 1 → chapter  : Calibri Bold 18pt
% Heading 2 → section  : Calibri Bold 16pt
% Heading 3 → subsection   : Calibri Bold 14pt
% Heading 4 → subsubsection: Calibri 12pt
% Heading 5 → paragraph    : Calibri 12pt
\usepackage{titlesec}

\titleformat{\chapter}[block]
    {\sffamily\bfseries\fontsize{18}{22}\selectfont}
    {\thechapter.}{0.5em}{}
\titlespacing*{\chapter}{0pt}{0pt}{18pt}

\titleformat{\section}
    {\sffamily\bfseries\fontsize{16}{20}\selectfont}
    {\thesection}{0.5em}{}
\titlespacing*{\section}{0pt}{18pt}{6pt}

\titleformat{\subsection}
    {\sffamily\bfseries\fontsize{14}{18}\selectfont}
    {\thesubsection}{0.5em}{}
\titlespacing*{\subsection}{0pt}{14pt}{4pt}

\titleformat{\subsubsection}
    {\sffamily\fontsize{12}{15}\selectfont}
    {\thesubsubsection}{0.5em}{}
\titlespacing*{\subsubsection}{0pt}{12pt}{4pt}

\titleformat{\paragraph}[hang]
    {\sffamily\fontsize{12}{15}\selectfont}
    {\theparagraph}{0.5em}{}
\titlespacing*{\paragraph}{0pt}{10pt}{4pt}

% --- Cabeçalhos e rodapés — Calibri 10pt ---
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[LE]{\sffamily\fontsize{10}{12}\selectfont\leftmark}
\fancyhead[RO]{\sffamily\fontsize{10}{12}\selectfont\rightmark}
\fancyfoot[C]{\sffamily\fontsize{10}{12}\selectfont\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0pt}

\fancypagestyle{plain}{
    \fancyhf{}
    \fancyfoot[C]{\sffamily\fontsize{10}{12}\selectfont\thepage}
    \renewcommand{\headrulewidth}{0pt}
}

% --- Gráficos e imagens ---
\usepackage{graphicx}
\graphicspath{{images/}}

% --- Cores ---
\usepackage{xcolor}
\definecolor{azulIPG}{HTML}{00467F}

% --- Tabelas ---
\usepackage{booktabs}
\usepackage{array}
\usepackage{longtable}

% --- Matemática ---
\usepackage{amsmath}
\usepackage{amssymb}

% --- Código fonte — monospace Courier New via fontspec ---
\usepackage{listings}
\lstset{
    basicstyle=\ttfamily\small,
    keywordstyle=\bfseries,
    commentstyle=\itshape,
    stringstyle=\color{black},
    numbers=left,
    numberstyle=\tiny,
    numbersep=8pt,
    breaklines=true,
    frame=single,
    framexleftmargin=15pt,
    captionpos=b,
    tabsize=4,
    showstringspaces=false
}

% --- Hiperligações ---
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    citecolor=black,
    urlcolor=black,
    bookmarksnumbered=true,
    pdfauthor={\nomeAutor},
    pdftitle={\tituloRelatorio},
    % Crédito ao criador base do template nos metadados do PDF (não remover):
    pdfcreator={Template LaTeX IPG criado por Vagner Bom Jesus (@VagnerBomJesus) -- github.com/VagnerBomJesus/TEMPLATE-IPG-LATEX},
    pdfproducer={XeLaTeX -- Template base de Vagner Bom Jesus (@VagnerBomJesus)}
}

% --- Aspas (recomendado pelo biblatex; carregar antes) ---
\usepackage{csquotes}

% --- Bibliografia ---
% =============================================================================
% ESTILO DAS REFERÊNCIAS — escolher UM. Deixar um bloco ativo e comentar (%)
% todos os outros. Depois de trocar, apagar os ficheiros auxiliares e
% recompilar com: xelatex -> biber -> xelatex -> xelatex  (ou latexmk -xelatex).
% Todos os blocos mantêm natbib=true para que \citet e \citep continuem a
% funcionar; em IEEE o comando idiomático é \cite (dá [1], [2], ...).
% =============================================================================

% ---- ATIVO: IEEE (numérico, citações [1], por ordem de citação) ----
\usepackage[
    backend=biber,
    style=ieee,
    sorting=none,
    natbib=true,
    maxbibnames=99
]{biblatex}

% ---- OPÇÃO: APA 7.ª ed. (autor-data; requer o pacote biblatex-apa) ----
% \usepackage[
%     backend=biber,
%     style=apa,
%     sorting=nyt,
%     natbib=true,
%     maxbibnames=99
% ]{biblatex}

% ---- OPÇÃO: Autor-data / Harvard (NP 405 — era o estilo anterior) ----
% \usepackage[
%     backend=biber,
%     style=authoryear,
%     sorting=nyt,
%     natbib=true,
%     maxbibna